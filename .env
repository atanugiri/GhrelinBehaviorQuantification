# Copy this file to .env and fill values for your environment.
# Do NOT commit your .env with real passwords or IPs.
# Copy this file to .env and fill values for your environment.
# Do NOT commit your .env with real passwords or IPs.

# PostgreSQL connection settings. You can either set DATABASE_URL (preferred)
# in the form: postgres://user:password@host:port/dbname
# or set the individual components below.
DATABASE_URL=
# Recommended local DB name when restoring the remote dump into your laptop
# (safe default to avoid overwriting an existing `deeplabcut_db`).
DB_NAME=deeplabcut_db_local
DB_USER=atanugiri
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# Local fallback data directory for CSVs used when DB is not available.
# Set this to a path that contains dlc_table_*.csv and other CSVs referenced
# by the project. Example: /path/to/shared/data
DATA_DIR=

# Optional: path to a local CSV for dlc_table if you prefer file-first workflows
# DLC_TABLE_CSV=/path/to/dlc_table_saline.csv

# Notes:
# If you pulled a remote DB dump (e.g. `deeplabcut_db.remote.dump`) and restored it
# locally into `deeplabcut_db_local`, set DB_NAME=deeplabcut_db_local above and
# DB_HOST=localhost so notebooks connect to your local copy. Keep your `.env` out
# of version control; use `.env.example` as the template.
